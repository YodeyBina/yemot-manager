<div dir="rtl">

# יומן שינויים

הפורמט: [Keep a Changelog](https://keepachangelog.com/he/1.1.0/).
מספור גרסאות: [Semantic Versioning](https://semver.org/lang/he/).

---

## [2.2.0-beta.3] — 2026-08-02

תיקוני באגים בלבד, מתוך בדיקת עומק שנייה מול שני קווים אמיתיים —
אחד לכתיבה ואחד עשיר בנתונים (33 שלוחות, שלושה חודשי לוג, 7,490
רשומות האזנה).

### אבטחה

- **הטוקן של הקו נשלח לספק ה-AI בכל שיחה.** במודול `api` נהוג להעביר
  את הטוקן כפרמטר בתוך `ext.ini` (`api_add_0=token=מערכת:סיסמה`),
  ומפת המערכת שנשלחת לעוזר נבנית מקובצי ההגדרות — ולכן סיסמת המערכת
  יצאה לספק חיצוני בכל פנייה, בשקט. בקו שנבדק היא הופיעה שם 10 פעמים.
  ההבטחה ש"הטוקן אינו נשלח למודל" היתה נכונה רק לאישורים השמורים
  בכספת המוצפנת, לא לסודות שיושבים בקבצים של המשתמש עצמו.
  הערך מוסתר עכשיו, והמבנה נשמר כדי שהעוזר יוכל עדיין לייעץ על ההגדרה.
  **מי שהשתמש בעוזר עד כה — כדאי להחליף את סיסמת המערכת.**
- **סריקת הפרטיות שלפני ייצוא בדקה רק שמות קבצים.** קובץ שמכיל סיסמה
  לא סומן, ולכן ייצוא, שכפול או פרסום היו מוציאים שליטה מלאה בקו.
  הסריקה קוראת עכשיו גם את התוכן, והממצא מוצג בנפרד — לא כ"מידע
  אישי" אלא כסוד, ובלי תיבת "כלול גם אותם" שלא סיננה אותו ממילא.

### תוקן

- **כישלון הוצג בצבע של הצלחה.** פעולה שנכשלה חלקית קיבלה התראה
  ירוקה, וכך גם "הוחלף ב-3 קבצים · 2 נכשלו". קישור "בטל" נשאר בשני
  המקרים — זו כל הנקודה בכישלון חלקי.
- **סריקת התקינות לא יכלה לזהות תפריט בלי הודעה מוקלטת.** רשימת
  הקבצים נבנתה בלי לסנן את הודעות ברירת המחדל של ימות, ולכן `M1000`
  שאינו קיים "כיסה" על הממצא. בקו שנבדק 81% מהרשומות היו כאלה.
- **מסך האבטחה הצהיר על כיסוי שלא היה** — "מתוך 48 שלוחות" בעוד
  שנסרקו 28, כי תיקיות המערכת נספרו אף שהלולאה מדלגת עליהן.
- **השוואת הקווים הציגה את סל המיחזור כהבדל מבנה**, ודחקה החוצה את
  ההבדלים האמיתיים.
- **חיפוש המודולים החזיר אפס על ניסוח נכון.** "תיבה קולית" לא מצא את
  "תא קולי", ו"שלוחת הקלטה" לא מצא את "הקלטות". נוספה התאמה ברמת
  מילה ומילון ניסוחים נרדפים. גם הדירוג תוקן: "תפריט" החזיר קודם את
  "בחירת מצב תפריט קבצים" לפני `menu`.
- **הודעות אנגליות של ימות הגיעו למסך כמות שהן** (`text file required`,
  `queuePath is required`), ופרמטר חובה חסר חזר כשגיאה אטומה בלי
  פירוט. הכול מתורגם לעברית עם מה לעשות.

## [2.2.0-beta.2] — 2026-07-31

תיקוני באגים בלבד, כולם מתוך בדיקת עומק מול קו אמיתי. אין שינוי
בהתנהגות שנועד להיות מורגש חוץ מאלה שלהלן.

### תוקן

- **סל המיחזור נסרק למרות שהיה אמור להידלג.** סל המיחזור של ימות נקרא
  `Trash`, וברשימת התיקיות לדילוג הוא הופיע כ-`trash`. ההשוואה היתה
  רגישה לאותיות ולכן לא התאימה כלל. בקו שנבדק זה היה **13,559 מתוך
  15,957 הרשומות במראה המקומית (85%)**. ההשלכות: סנכרון איטי ומאגר
  מקומי מנופח, תוצאות חיפוש מזוהמות בשלוחות מחוקות, ו**ייצוא שכלל את
  סל המיחזור גם כשלא סומן לכלול אותו**.
  **שדרוג מגרסה קודמת מנקה את עצמו:** סריקה שמדלגת על סל המיחזור לעולם
  לא רושמת אותו מחדש, ולכן גם לא היתה גוזמת את השורות הישנות — מי שסנכרן
  פעם אחת לפני התיקון היה נשאר עם הזיהום לתמיד. הסנכרון מוחק אותן עכשיו
  בעצמו, בלי צורך ב"נקה מטמון מקומי".
- **סריקת התקינות דיווחה בעיקר על שלוחות מחוקות.** הדילוג בדק רק את שם
  התיקייה האחרון בנתיב, ולכן `Trash` דולגה אבל `Trash/Delete/6c` לא.
  בקו שנבדק **14 מתוך 15 הממצאים** הגיעו מסל המיחזור, והממצא האמיתי
  היחיד נקבר תחתיהם.
- **`menu_star` דווחה כ"סוג שלוחה לא מוכר".** זו שלוחת מערכת שימות
  יוצרת בעצמה בכל קו (תפריט הכוכבית), והיא אינה מודול מתועד ולכן לא
  היתה בקטלוג. ממצא כוזב שהופיע בכל מערכת.
- **ספירת הקבצים כללה קבצים שאינם קיימים.** ימות מחזירה כ-350 הודעות
  ברירת מחדל לכל שלוחה עם `exists=false`. לוח הבקרה הציג בגללן
  "463 קבצים · 89 B" במקום שני קבצים בגודל 89 בתים.
- **`GetScheduledCampaigns` נכשלה תמיד.** הפרמטר `type` הוא חובה
  (`PENDING`/`SUCCESSFUL`/`FAILED`) והפקודה נשלחה בלעדיו.
- **שגיאות בלי הודעה קרסו ל"שגיאה לא ידועה".** כשימות מחזירה כישלון עם
  הודעה ריקה, `exceptionClass` הוא המידע היחיד שיש — והוא נזרק.
- **שגיאות בנוסח `Exception` נבלעו בשקט.** ימות מחזירה גם `EXCEPTION`
  וגם `Exception`, וההשוואה היתה רגישה לאותיות — כך שכישלון בנוסח
  השני הוחזר לקוד הקורא כאילו הפעולה הצליחה.
- **דיווח כישלון ב-TTS נקב במודל השגוי.** ההודעה הציגה את מודל הצ'אט
  של הפרופיל ("gemini-2.5-flash חרג ממכסה") בעוד שיצירת הקול רצה על
  מודל ה-TTS. המשתמש נשלח לשנות מודל שכלל לא השתתף.
- **תאריכים הוצגו כמספר גולמי.** "תוקף: 1815771600000" בלוח הבקרה
  ובהשוואת הקווים, ו"סנכרון אחרון: 1785445369" באבחון.
- **"קבצים לא בשימוש" הציע למחוק הקלטות של מאזינים.** קובץ בתבנית
  `0534181207_1783241273143.wav` הוא תוצר של שיחה ולעולם אינו מוזכר
  ב-`ext.ini`, ולכן סומן כיתום. נוספו גם שני סוגי שלוחה שהיו חסרים:
  `api` משמיע קבצים ממוספרים החל מ-000 (כך בתיעוד הרשמי), ובשלוחת
  `music_on_hold` הקובץ שבתוכה *הוא* מוזיקת ההמתנה. בקו שנבדק **כל
  עשרת הממצאים היו שגויים**, ובהם 6MB של הקלטות אמיתיות.
- **"מה חדש" הציג רשימה ישנה תחת מספר גרסה חדש.** הכותרת הזריקה את
  הגרסה הנוכחית מעל תוכן קשיח. יש עכשיו `WHATS_NEW_VERSION` ובדיקה
  שנכשלת כשהוא אינו תואם לגרסת התוכנה.
- **הייצוא ניסה להוריד קבצים שאינם קיימים.** הודעות ברירת המחדל של
  ימות (`exists=false`) נכללו בתוכנית הייצוא, וכל אחת נוסתה בקריאת רשת
  נפרדת שנכשלת. ייצוא של שלוחה אחת עם 8 קבצים ניסה להוריד **948** ולקח
  דקות; אחרי התיקון — 12 קבצים ב-0.2 שניות, בלי אזהרות. על קו מלא אלה
  היו עשרות אלפי קריאות מיותרות מול שרת שמגביל קצב.
- **ביטול פעולה יצר "חור" במראה המקומית.** `refresh_paths` קרא ל-`forget`
  על כל נתיב שהושפע, גם כשהקובץ עדיין קיים בקו. עד הסנכרון הבא כל מי
  שקורא את המראה במלואה — ייצוא, חיפוש, סריקת יתומים — לא ראה אותו,
  ולכן **קובץ אמיתי נעדר מחבילת הגיבוי בלי שום סימן**. `forget` שמור
  עכשיו למחיקה או להעברה שבאמת קרו, כפי שמתועד ב-`Mirror.invalidate`.
- **שגיאת `error` של ימות הועברה למשתמש כמות שהיא.** `FileAction` על
  נתיב שאינו קיים מחזירה את המילה `error` בלבד; היא אינה ריקה ולכן
  עברה את הסינון. מוצג עכשיו הסבר בעברית עם מה לבדוק.
- **קובץ שמע בשם עברי לא ניתן היה להשמעה או לעריכה.** כותרות HTTP
  מקודדות ב-latin-1, ושם הקובץ הגולמי נכנס ל-`Content-Disposition`
  כמות שהוא — הבקשה מתה ב-`UnicodeEncodeError` בלי תשובה והדפדפן נתקע.
  זה השם הנפוץ ביותר בכלי הזה. הכותרת נבנית עכשיו לפי RFC 6266.
- **פעולה מרובה שנכשלה חלקית ביצעה — ולא השאירה דרך לבטל.** `FileAction`
  של ימות מבצע את מה שהוא יכול ומדווח כישלון על השאר. הקוד רשם את צעדי
  הביטול **אחרי** הקריאה, ולכן כל חריגה הגיעה ל-`abort()` ומחקה את
  תמונות המצב שכבר נלכדו. במדידה מול הקו: מחיקה של שני פריטים שאחד מהם
  אינו קיים **מחקה את הפריט התקין** והשאירה אותו בלתי ניתן לשחזור.
  שלושת המסלולים — מחיקה, העתקה והעברה — שומרים עכשיו את הרשומה גם
  בכישלון, מחזירים `partial: true` עם `undoId`, והממשק מציע ביטול.
- **מסך האבטחה הציג "אין סשנים פעילים" כשלא הצליח לשאול.** ימות מחזירה
  `Unverified session` לפקודות `GetAllSessions` ו-`GetLoginLog` — הטוקן
  של התוכנה אינו סשן מאומת. הסירוב נבלע והמסך הציג טבלה ריקה, כלומר
  אמר למשתמש שאין מה להראות במקום שלא הצלחנו לשאול. ההודעה מתורגמת
  עכשיו לעברית ומוצגת במסך.
- **ביטול דרס שינוי שנעשה מחוץ לתוכנה.** עריכה בתוכנה, שינוי של אותו
  קובץ באתר הניהול או בטאב אחר, ואז ביטול — והשינוי החיצוני נמחק עם
  `ok=true` ובלי אזהרה. אותה מחלקת כשל של הסעיף הבא, ממקור אחר: כאן
  אין רשומה מאוחרת יותר, ולכן הבדיקה משווה את מה שהרשומה שמרה כמצב
  *אחרי* הפעולה מול מה שיש בשרת עכשיו.
- **ביטול רשומה ישנה מחק שינויים מאוחרים יותר, בשקט.** היומן מאפשר
  לבטל כל רשומה ולא רק את האחרונה — וזה מכוון — אבל ביטול משחזר את
  המצב שקדם לרשומה, ולכן מוחק כל שינוי שנעשה אחריה על אותם קבצים.
  במדידה: שלוש עריכות של אותו קובץ, ביטול הראשונה, **והקובץ נמחק
  מהשרת** יחד עם שתי העריכות שאחריו — עם `ok=true` ובלי שום אזהרה.
  הביטול עוצר עכשיו, מפרט אילו פעולות יאבדו, ופועל רק באישור מפורש.
- **הגדרות שלוחה נשמרו בשרת אך לא במראה.** אחרי שמירה בטופס ההגדרות,
  עורך ה-ext.ini הגולמי הציג את הקובץ כפי שהיה **לפני** השמירה, ועריכה
  מתוכו נחסמה כ"מישהו אחר שינה את הקובץ" — על שינוי שהמשתמש עשה בעצמו
  רגע קודם. (הגנת הדריסה עצמה תקינה: היא קוראת מהשרת ולכן לא היה
  אובדן נתונים.)
- **התרעה כוזבת "מחיקה זו לא תהיה ניתנת לביטול".** התצוגה המקדימה ספרה
  גם את הודעות ברירת המחדל של ימות: תת-עץ עם 7 קבצים אמיתיים במשקל 359
  בתים דווח כ-**829 קבצים**, וכל תפריט עם שלוש תת-שלוחות כבר חצה את
  תקרת 500 וקיבל את האזהרה. הגיבוי בפועל דווקא הצליח — כלומר המשתמש
  הוזהר לשווא שלא יוכל לבטל, בדיוק במנגנון שנועד להרגיע אותו.
- **הבטחת "אפשר לבטל" נשענה על אפס מידע.** פריט שאינו במראה החזיר
  `undoable=true`, והממשק הבטיח למשתמש שאפשר יהיה לבטל. אם התת-עץ חורג
  מהתקרה זה התגלה רק בזמן המחיקה — בדיוק מה שהמנגנון נועד למנוע. יש
  עכשיו מצב שלישי, "לא ידוע", עם המלצה לסנכרן לפני.
- **עמודת "תיאור" בדוח המערכת היתה ריקה בכל שורה.** הדוח קרא מפתח
  שאינו קיים (`label` במקום `he`), ולכן כל מודול הוצג בלי שם — בדוח
  שנועד בדיוק לכך שמי שמתחזק את הקו יבין מה כל שלוחה עושה. גם
  "סנכרון אחרון" בדוח הוצג כחותמת זמן גולמית.
- **כותרת `Content-Type` כפולה** בדוחות ובדף הראשי
  (`charset=utf-8; charset=utf-8`) — נובע מהעברת charset בתוך `mimetype`
  במקום ב-`content_type`.
- **סל המיחזור הוצג כתיקייה ריקה במקום להיטען מהשרת.** אחרי ניקוי
  התוכן נשארה רשומת סנכרון שאמרה "נסרקה, ואין בה כלום", ומי שנכנס
  אליה קיבל תשובה שקרית בשקט.
- **בטלפון היו כפתורים שאי אפשר היה להגיע אליהם.** שורת הלשוניות
  נחתכה במסך צר בלי אפשרות גלילה, ו"⚙ הגדרות השלוחה" — הכפתור המרכזי
  בסייר — יצא מחוץ למסך. בנוסף הסרגל הצדדי נפתח כברירת מחדל וכיסה 268
  מתוך 375 פיקסלים (71% מהמסך), וגם חסם לחיצות על מה שמתחתיו. הסרגל
  נסגר עכשיו אוטומטית במסך צר ואחרי בחירה בניווט, והלשוניות נגללות.
- **SMS, צינתוק ופקס החזירו שגיאות באנגלית.** לא היה שום אימות מקומי,
  ולכן קלט חסר נשלח לימות והיא ענתה `Missing parameters` — הודעה
  שהועברה למשתמש כמות שהיא, בלי לומר מה חסר ומה לעשות. דווקא במסכים
  שבהם הכי קל לטעות בהקלדת מספרים.
- **עורך השמע איבד עריכות בשקט.** "סגור" סגר מיד גם כשהיו שינויים
  שלא נשמרו, בניגוד לכל שאר המסלולים ההרסניים בתוכנה. יש עכשיו אישור
  עם "חזור לעריכה".

### נוסף

- **המאמת תופס שגיאות כתיב במפתחות ext.ini.** `titel=` במקום `title=`
  פשוט לא נקרא בימות, בלי שום הודעת שגיאה — הכשל השקט הקלאסי. ההתרעה
  מוגבלת לדמיון מובהק (מרחק עריכה 1, כולל החלפת תווים סמוכים) ונבדקה
  מול 1,473 מפתחות מהתיעוד הרשמי בלי אף התרעה כוזבת.
- **אזהרה על קובץ שלא יופיע בסייר.** ימות אינה מחזירה קובצי `.txt`
  ברשימת התיקייה, למרות שהם נשמרים בקו וניתנים לקריאה. עד כה הקובץ
  פשוט "נעלם" אחרי רענון וזה נראה כמו באג בתוכנה.

---

## [2.2.0-beta.1] — 2026-07-28

גרסה שנבנתה כולה מול משוב מהפורום ומבדיקות על קו אמיתי.

### נוסף

- **עורך שמע בדפדפן** — גל קול, סימון בגרירה, חיתוך, מחיקת מקטע,
  השתקה, דעיכה, הגברה, נרמול, שינוי מהירות בלי לשנות את גובה הקול,
  זום, האזנה לתוצאה, ושמירה כדריסה או כקובץ חדש.
- **שרשרת מפתחות ומודלים** — כשמפתח מגיע למכסה או נכשל, התוכנה עוברת
  אוטומטית לבא בתור. הסדר נקבע על ידכם, ופרופיל שמוצה מסומן.
- **אשף הוצאת מפתח Gemini** — פותח את דף היצירה, מסביר בעברית, ומאמת
  מיד אילו מודלים באמת עובדים למפתח.
- **תמלול, סיכום וניתוח הקלטות** — תמלול מלא, סיכום, נושאים, משימות
  לביצוע, ופרטים ומספרים שנאמרו.
- **סטטיסטיקות האזנה** לכל שלוחה: כניסות, מאזינים, זמן האזנה, פילוח
  לפי יום ושעה, דירוג תת-שלוחות והשוואה לחודש הקודם.
- **כלי עריכת טקסט להקראה** — ניקוד, הסרת ניקוד, ניסוח מחדש, הרחבת
  מספרים ותאריכים למילים, קיצור, סיכום, ותרגום לעברית/אנגלית/יידיש/
  רוסית/צרפתית.
- **ייבוא תיקייה שלמה** מהמחשב, כולל יצירת מבנה השלוחות והכינויים.
- **שינוי סדר השמעה בגרירה**, עם מספור מחדש בטוח ואימות התוצאה.
- **בניית הודעת תפריט** אוטומטית מהכינויים של תתי-השלוחות, ויצירת
  `TitleVoice.ini`.
- **סריקת תקינות** שמאתרת יעדי ניתוב שאינם קיימים וקבצים חסרים.
- **כללי כניסה בממשק חזותי**, עם הסבר בעברית מה יקרה למי שמתקשר.
- **דירוג רמת סיכון** לכל פעולה בתוכנית AI לפני האישור.
- **בחירת מפתח באולפן הקול** ושליטה בטמפרטורה של המודל.
- **שליחת SMS לרשימות תפוצה** עם תחליפים ותצוגה מקדימה.
- **הודעות מערכת** מוצגות עם טקסט ברירת המחדל שלהן (3,320 הודעות).
- **מתקין ל-Windows** עם קיצור בתפריט התחל, בלי צורך בהרשאת מנהל.
- **מסך מלא**, מפריד נגרר לעץ השלוחות, ופתיחת כל הענפים.

### תוקן

- **כל העלאה דווחה ככישלון** אף שהקובץ נכתב בהצלחה. התוכנה שלחה קריאת
  סיום ששייכת רק להעלאה מפוצלת. נגע ב-TTS, בהעלאת קבצים ובעורך השמע.
- **תיאורי הודעות המערכת מעולם לא הוצגו** — אי-התאמה בין שם עם סיומת
  לבין מפתח בלעדיה.
- **חלל ריק בעץ השלוחות**: כל שלוחה בלי תת-שלוחות תפסה 123 פיקסלים
  במקום 25, בגלל התנגשות שמות ב-CSS.
- **דוח התנועות הוצג מעוות** — כתובות IP וסכומים התערבבו בטקסט עברי.
- מודל TTS שנכשל בשגיאה שלו סווג כבקשה פסולה ועצר את שרשרת הגיבוי.

### שונה

- ברירת המחדל היא **מצב בהיר**, ולשונית **שלוחות** נפתחת ראשונה.
- סוג השלוחה מוצג בלי הקידומת `type=`.
- העוזר מכיר את הממשק ויודע להדריך איפה בדיוק ללחוץ.

---

## [2.1.0-beta.1] — 2026-07-26

הגרסה הציבורית הראשונה.

### נוסף

- **ביטול והחזרה לכל פעולה** (`Ctrl+Z` / `Ctrl+Shift+Z`) — כולל מחיקת
  שלוחה שלמה, תוכנית AI וייבוא חבילה. גיבוי מקומי נשמר לפני כל שינוי,
  ולפני מחיקה גדולה התוכנה אומרת מראש אם אפשר יהיה לבטל.
- **יומן פעולות** (🕘) עם ביטול/החזרה לכל רשומה, ומרכז התראות (🔔).
- **טיוטות אוטומטיות** בעריכת קבצים, ו**הגנה מדריסה** כששני טאבים
  פתוחים על אותו קובץ.
- **כלים על כל הקו**: אבחון התוכנה, בדיקת אבטחה, קבצים שאינם בשימוש,
  חיפוש-והחלפה גלובלי, השוואת קווים, ייצוא מפת המערכת כדוח.
- **שיפורי AI**: בדיקת מפתח שמגלה אילו מודלים באמת זמינים לו, תבניות
  מערכת מוכנות, אומדן עלות לתוכנית, השוואת קולות, יצירת שמע באצווה.
- **מאגר קהילתי** (רשות, כבוי כברירת מחדל): הערות על מודלי AI וקטלוג
  מערכות ששותפו.
- בדיקת חיבור לפני שמירת קו, שכפול הגדרות שלוחה, סידור סדר השמעה
  בגרירה, מסנני חיפוש, קונטרסט גבוה, שינוי גודל גופן, תצוגה למסך צר.
- **עדכון מתוך התוכנה** — זיהוי גרסה חדשה בגיטהאב והורדה בלחיצה.
- **תנאי שימוש** שהמשתמש מאשר בהפעלה הראשונה, ומסך "אודות" (ⓘ).

### תוקן

- **מחיקה לא מחקה בפועל.** `GetTextFile` על קובץ חסר מחזיר שגיאה בשרת
  האמיתי (ולא תוכן ריק, כפי שהניחו). הגיבוי שלפני המחיקה נפל, והמחיקה
  מעולם לא רצה.
- **תשובות ה-AI הגיעו כג'יבריש.** ספקי ה-AI שולחים `text/event-stream`
  בלי `charset`, ואז `requests` נופל ל-ISO-8859-1 ועברית מתפרקת.
- **חיבור נחסם על ידי אנטי-וירוס** שסורק HTTPS (Avast וכדומה). התוכנה
  טוענת עכשיו את אישורי השורש של מערכת ההפעלה.
- **כישלון חלקי ב-`FileAction`** (מחיקה/העתקה של כמה פריטים) נראה
  כהצלחה מלאה — כל פריט נבדק עכשיו בנפרד.
- **כתיבה לשלוחה שאינה קיימת** החזירה `OK` בלי לכתוב כלום. עכשיו זו
  שגיאה מפורשת.
- `CheckIfFileExists` נקרא מהמפתח הלא נכון ולכן תמיד החזיר "לא קיים".
- ביטול מחיקת שלוחה החזיר אותה בלי קבצי ההגדרות שבתוכה.
- האטה אוטומטית כשהשרת מגביל קצב (429), במקום כישלון.

### שונה

- שרת הדמה שבבדיקות יושר להתנהגות השרת האמיתי — הוא היה "נדיב" יותר,
  ולכן הבדיקות אישרו התנהגות שלא קיימת בשטח.
- מכסה למטמון השמע המקומי (2GB), עם גיזום של מה שלא נגעו בו מזמן.

</div>
